package com.expression.evaluator.operator;

/**
 * @author rohitkumar
 * creation date 22/07/18
 * project name expression-evaluator
 */
public class Not implements Operator<Boolean, Boolean> {

    /**
     * TODO Not only takes one operand. Need to think over it.
     * @param left operand
     * @param right operand or value
     * @return
     */
    @Override
    public boolean execute(Boolean left, Boolean right) {
        return (left );
    }
}
